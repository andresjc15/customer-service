
spring:
  application:
    name: customer-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/customer_service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    status-page-url-path: /swagger-ui/

server:
  port: ${PORT:0}

path:
  enterprise-costumers: /api/enterprise-customers
  personal-costumers: /api/personal-customers

#logging:
#  config: K:/bootcamp/proyecto1/customer-service/src/main/resources/logback.xml

