
spring:
  application:
    name: customer-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888

eureka:
  instance:
    hostname= localhost

management:
  endpoints:
    web:
      exposure:
        include: '*'
